class plan {
  String chitPlan, monthlyAmt, tenor, type, eMI;
  List amount;

  plan(
      {this.chitPlan,
      this.amount,
      this.monthlyAmt,
      this.tenor,
      this.type,
      this.eMI});
}
